import React from 'react';
import { HeaderContainer, HeaderTitle, HeaderSubtitle } from './styles';

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>Biblioteca Musical</HeaderTitle>
      <HeaderSubtitle>Tu colección de canciones favoritas</HeaderSubtitle>
    </HeaderContainer>
  );
};

export default Header;